# 비준 기록 — Physics E1 설계 동결 (JJ, 2026-08-01)

**JJ 비준 완료.** 이 문서는 동결 시점의 앵커다 — 사전등록의 요체는 "언제
무엇이 얼려졌나"이므로, PREREG는 이 기록 위에 선다.

## 비준 대상 (둘의 합)

| 문서 | 커밋 | 내용 |
|---|---|---|
| `SPEC_physics_e1_v2.md` | `2e74a5cd` | 4조건 축약 · 회수율 추정량 · 선정-독립 규칙 · P1b 영수증 · `scribe_importance` 사전등록 |
| `drafts/ray_physics_e1_round3_20260801.md` | `8ae3aff4` | 결정 3건 (단일-풀 harm-주력 · 확증-NHST-없는 measured-profile · 164런) |

**충돌 시 우선순위: Ray 라운드-3 > v2.** 라운드-3이 v2의 세 지점을 명시
개정했다 — P1a 밴드/help 셀 폐기(→ 단일 풀), "haiku C3 = 단독 확증" 강등
(→ 주 대비), n 배분(→ 164런).

## 절차 이력 (역할-분담 합의 절차, 2번째 실적)

1. 초안 `ac6c21f2` (Cody)
2. 반론 `b645b691` (LxM Challenger, R1–R5, 데이터 첨부)
3. 수정 `ce05369a` (Ray, R1–R5 전부 수용 + **정정 1** on-record)
4. v2 `2e74a5cd` (Cody, 축약 + §4 반론)
5. 회신 `d5df4c85` (LxM, A/B — C3 미이식 · 회수율 = 파이프-피델리티)
6. 판정 `8ae3aff4` (Ray, **정정 2** on-record + 결정 3건)
7. ACK `82ddb354` (LxM, Challenger 역할 종료)
8. **비준 (JJ, 2026-08-01) ← 여기**

Ray 자평: *"두 번 다 'arm에 실체를 주고 싶다'가 '벽이 물 수 있나'를
앞질렀다."* 동결 전 반론 라운드가 잡아낸 것이 R1(뒤집힌 carriage)과
§4(바닥-퇴화) 둘 다 **설계상 측정 불능**이었다 — 절차가 값을 했다.

## 다음: PREREG (작성자 = Ray)

Ray 라운드-3 §Next가 열거한 산출물:

- 사전값 수치 확정 (방향만 기록됨: recovery ≥0.8 ~65% · harm 검출 ~25% ·
  C3>C0 PASS ~90%)
- W0 프로토콜 (스크리닝 16 env × 2 계보 × C1·C0 k=1 = 64런; 풀 =
  screen-C1-성공 ∧ screen-C0-실패, 목표 계보당 10 env, 8 미만이면 +8 env
  추가 — 사전 규칙, 재량 없음)
- VOID 의미론
- f-carriage-phys / 역-carriage assert 목록
- 분석 사전-커밋 (양방향 exact p + 방향 분해)

### PREREG에서 Ray가 닫을 미결 1건 — n 배분 비대칭

배터리 80런이 네 조건에 **균등** 배분인데 조건별 기대 정보는 극단적으로
불균등하다: C3>C0은 ~90% PASS 예상, 회수율은 ~1.0 기대, **양 랩이 최고-정보로
합의한 harm(C2 vs C1)만 n=10 페어**라 LARGE harm만 바운드된다. run이 1콜이라
재배분은 싸지만, env를 늘리면 **스크리닝 비용이 지배**하고(풀 수율 미지수)
env당 반복(k=2)이면 검정 형태가 바뀐다 — 어느 쪽이든 PREREG에서 확정한다.
**균등 유지도 답이다** (그 경우 T1-언어로 검출 한계를 명시).

## 동결된 것 (런타임 재량 없음)

- `scribe_importance` = **0.8** — 0.6이면 인출 0/5, 0.8이면 5/5 (실측 2×2).
  한 숫자가 arm을 전무↔완전 사이에서 뒤집으므로 런타임 선택 금지.
- `sample_quota` = **5** (`recall_n`=5), 관측 1줄 ≤ 400자
- 스토어 = run당 신선 (세션 경계만 관통, run 경계 불관통)
- 가설 제출 = **단-샷 1회**
- 마스킹 = anonymous 고정 (C0–C3), default는 별도 서술 앵커
- 표본 설계 = 시드 고정, 네 조건 전부 **동일 관측 집합** (드라이버 고정 설계)
- 계보 = haiku(주 대비) · grok(등록 복제), **agy 제외**
- **스크리닝 런 ≠ 배터리 런** (선정-독립)

## 사전 명시된 walk의 형태

> **확증 NHST 없는 measured-profile walk.** 남은 주장 중 어느 것도 확증의
> 모양이 아니다 — C3>C0은 예상 강한 PASS(등록 복제-급 확장), 회수율은 구성상
> ~1.0 기대(프로파일), harm만 진짜 불확실(등록 탐색). 예상-PASS에 확증을
> 씌우면 서스펜스 제조고, 예상-null에 우월성 검정을 씌우면 모양이 틀렸다.
> immune P1 선례대로 정직하게 출항한다.

수확 예상: 회수율 프로파일 + harm 바운드 + **per-lineage anonymous-귀납
literacy 데이터**(R4 층화축이자 LxM 외부-축 화물) + physis-공백 소견.
가장 큰 수확 하나는 **출항 전에 이미 떨어졌다** — `scribe_importance` 절벽은
memory organ의 실측 결함이고 개선-레인 표본으로 등록됐다.

## 기록된 공백 (미측정, 후속 walk)

1. **physis에 브레인-대면 표면 없음** → "물리 organ이 물리를 돕나"는 현 구조로
   측정 불가. 표면화는 개선 레인.
2. **인출 판별력 미측정** — 신선 스토어면 인출이 구성상 완전(실측 5/5)이라
   C3는 memory를 파이프로 재지 판별기로 재지 않는다. distractor 조건 = 후속 walk.
3. **인출 기전 캐빗** — 실제로 태우는 채널은 어휘 매칭이 아니라
   recency-significance. verdict 문안에 "인출 완전은 신선-스토어 조건부"로 승격.
4. **양 랩 대조는 harness-강건성이 아님** (LxM ACK) — 외부모델-LxM하네스 vs
   크리처-Ludex하네스는 하네스 × agent-type 교란. 그 라벨로만 싣는다.

— 기록: Cody, 2026-08-01
