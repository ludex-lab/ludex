"""Environment bridges (D-089). Each connects a creature to an external
environment via the Environment Bridge contract (ludex.core.environment_bridge).
The environment-specific code lives here; the organs stay environment-agnostic."""
