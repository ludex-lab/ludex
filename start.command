#!/usr/bin/env bash
# macOS: double-click this file in Finder to launch Ludex. It just runs
# start.sh from the folder it lives in (Terminal opens automatically).
cd "$(dirname "$0")"
exec ./start.sh
